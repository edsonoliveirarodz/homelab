# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# Gerado pelo Terraform
ansible/inventory.yml

# Credenciais
terraform/*.tfvars

# Logs de execução
logs/

# CA private key - never commit
ansible/k8s-cluster-baremetal/files/pki/*.key
