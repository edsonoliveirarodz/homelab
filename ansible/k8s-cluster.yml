---
- name: Provisionar cluster Kubernetes
  hosts: k8s_cluster
  become: yes
  pre_tasks:
    - name: Wait for SSH
      ansible.builtin.wait_for_connection:
        timeout: 300
  roles:
    - k8s-cluster-baremetal
