---
- name: Install API Gateway CRDs
  ansible.builtin.command:
    cmd: kubectl apply --server-side=true -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v{{ gateway_api_version }}/experimental-install.yaml
  changed_when: true
...
